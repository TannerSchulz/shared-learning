apiVersion: v1
kind: Service
metadata:
  name: <service name>
spec:
  type: NodePort
  selector:
    app: <app name>
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30001 #starting port*